[
  {
    "id": "overview",
    "title": "What is Quartermaster",
    "keywords": [
      "about",
      "intro",
      "engine",
      "what",
      "swap",
      "local"
    ],
    "body": "Quartermaster is an all-in-one **local inference engine** - a front-end over llama.cpp (text/vision models) and stable-diffusion.cpp (images) that runs entirely on your own machine. Nothing is sent to a cloud service; weights, prompts, and conversations stay local.\n\nIt discovers the model files you have on disk, works out sensible runtime settings for each one automatically (context length, GPU offload, KV cache), and **hot-swaps** models in and out of VRAM on demand - a request for a model that isn't loaded triggers a load (evicting others if needed).\n\nTwo web UIs: the **operator dashboard** (main port - catalog, loading, config, metrics) and the **playground** (its own port - chat, images, speech, transcription, rerank)."
  },
  {
    "id": "updating",
    "title": "Updating Quartermaster",
    "keywords": [
      "update",
      "upgrade",
      "new version",
      "installer",
      "self-update",
      "version"
    ],
    "body": "Quartermaster can update itself (Windows release builds only). When a newer version is available, the **Sidebar** shows an update control.\n\n- Clicking it downloads and launches the release installer, then the server shuts down gracefully so the installer can replace it.\n- Both the dashboard and playground briefly drop while it restarts - reload the page once the installer finishes.\n- Only release builds wired with the updater show this; a dev / self-compiled build won't."
  },
  {
    "id": "loading-models",
    "title": "Loading and swapping models",
    "keywords": [
      "load",
      "unload",
      "swap",
      "start",
      "stop",
      "evict",
      "vram",
      "group"
    ],
    "body": "You don't manually start models most of the time - sending a request to a model (from the playground or an API client) **loads it on demand**. If another model occupies the VRAM it needs, Quartermaster stops that one first (eviction).\n\n- **Models page** (dashboard): browse the catalog, click a model to load/unload it explicitly.\n- **Idle unload**: a model with an unload timeout stops itself after being idle, freeing VRAM.\n- **Swap groups**: models in the same group can't be resident at once; loading one evicts its group-mates. Models in different groups (and enough VRAM) can coexist.\n- First load of a large model is slow (reading weights from disk); subsequent swaps are faster while the file is warm in OS cache."
  },
  {
    "id": "model-config",
    "title": "Per-model configuration (the cogwheel editor)",
    "keywords": [
      "config",
      "settings",
      "cogwheel",
      "context",
      "ctx",
      "offload",
      "variant",
      "tune",
      "params",
      "gpu layers",
      "backend",
      "vllm"
    ],
    "body": "On the Models page, the cogwheel opens the per-model config editor. It shows the exact launch command and lets you tune:\n\n- **Context size (ctx)**: how many tokens the model can hold. Bigger ctx uses more KV-cache VRAM.\n- **Target VRAM**: a budget; Quartermaster computes GPU offload (`-ngl` / `--n-cpu-moe`) to fit. Lower target = more layers on CPU = slower but fits smaller cards.\n- **Backend**: which engine runs this model (llama.cpp, vLLM, ...). Leave on auto or pin one; see the Backends article. Config is stored per-backend.\n- **Variants**: alternate profiles of the same model (e.g. different ctx tiers) selectable per request.\n\n**Save & reload** applies changes live - a running model keeps serving under its old settings and the new ones take effect on its next load. The staging card shows what a running model is *actually* loaded with, which can differ from the pending config until it reloads."
  },
  {
    "id": "config-variants",
    "title": "Config variants (normal vs fleet-wide)",
    "keywords": [
      "variant",
      "variants",
      "profile",
      "tier",
      "ctx tier",
      "high context",
      "low context",
      "fleet",
      "default",
      "alternate",
      "preset"
    ],
    "body": "A **variant** is a named alternate profile of a model - most often a **high- or low-context** version. Each variant surfaces as its own selectable entry (the variant name becomes an id suffix, e.g. `mymodel-32k` or `mymodel-128k`) and launches with its own settings - context size, VRAM target, KV type, speculative decoding, sampler, and the rest. Pick a variant in the playground's model selector; each loads and swaps independently, like any other model.\n\nWhy bother: a bigger context window holds more conversation/documents but costs more VRAM, so a low-context variant fits on a smaller card or leaves room for other models, while a high-context variant is there when you need the extra room.\n\n**Inheritance**: a variant only overrides the fields you set on it; anything left blank inherits the model-wide config. So a high-context variant can set just `ctx` and inherit everything else.\n\nTwo kinds, both edited in the cogwheel config editor:\n\n- **Normal (per-model) variants** - belong to one model and appear only on it. Use them for that model's context tiers (e.g. 32k / 64k / 128k) or a specially tuned profile of that one model.\n- **Fleet-wide (default) variants** - shared by **every** model, saved globally (not on any single model). Use them for a profile you want available everywhere, like a standard low- and high-context pair. They're edited in the same editor but saved separately, so a change applies across the whole fleet at once.\n\nAutogen can also emit **context-tier variants automatically** from a model's ctx tiers, so you often get 32k/64k/128k options without defining them by hand."
  },
  {
    "id": "autogen",
    "title": "Automatic config generation",
    "keywords": [
      "autogen",
      "generate",
      "auto",
      "defaults",
      "discover",
      "gguf",
      "estimate"
    ],
    "body": "Rather than hand-writing a config per model, Quartermaster **auto-generates** one at startup: it scans your model folders for GGUF/checkpoint files, estimates each model's VRAM footprint (weights + KV + compute buffers), and derives runtime flags (context, GPU offload, KV type) that fit your hardware.\n\nThis means adding a model is usually just dropping the file in the watched folder - no manual YAML. You can still override any of it per-model in the cogwheel editor, and those overrides survive regeneration."
  },
  {
    "id": "backends",
    "title": "Backends (llama.cpp, vLLM, sd, tts)",
    "keywords": [
      "backend",
      "backends",
      "llama.cpp",
      "vllm",
      "sd-server",
      "tts",
      "exe",
      "engine",
      "vulkan",
      "rocm",
      "hip",
      "cuda",
      "tensor parallel",
      "gpu-memory-utilization"
    ],
    "body": "**Settings -> Backends** is a list of inference engines Quartermaster can launch. Each row is one binary: a **kind** (llama, vllm, sd, tts, or custom), a name, and the path to its executable. Click **+ Add backend** for more (e.g. a Vulkan *and* a ROCm llama build, or CUDA llama + vLLM), **✕** to remove.\n\n**Auto-pick (★).** Each backend can be starred as the default for its class (llm / image / tts). When a model loads, Quartermaster picks a backend by class: the model's explicit choice wins, else the ★-starred one, else the first of that kind. So with one llama build everything just works; add more and the star decides the default.\n\n**Per-model override.** In the cogwheel editor each model has a **Backend** dropdown (filtered to the backends valid for its class). Leave it on auto or pin a specific engine. Config is *keyed to the backend*: a model's llama tuning (ctx / target VRAM / variants) and its vLLM knobs are stored separately, so switching engines never wipes the other set.\n\n**vLLM.** Selecting a vLLM backend launches `vllm serve <gguf> --quantization gguf --gpu-memory-utilization <util> --max-model-len <ctx> [--tensor-parallel-size N]`. It reuses the same discovered GGUF. vLLM self-manages VRAM, so the llama estimate panel and offload/KV planner are hidden - you get two knobs: **GPU utilization** (fraction of VRAM, default 0.90) and **tensor-parallel size** (GPUs to shard across). Note vLLM is slow to start and not every GGUF architecture runs on it."
  },
  {
    "id": "multi-listener",
    "title": "Multiple ports & per-port model catalogs",
    "keywords": [
      "listener",
      "listeners",
      "port",
      "ports",
      "catalog",
      "multi-listener",
      "groups",
      "scope",
      "cross-port",
      "eviction"
    ],
    "body": "Quartermaster can expose **several listen ports** from one process, each showing a different subset of models - e.g. an 'assistant' port and a separate 'tools/judge' port.\n\n- **Per-port catalogs**: a request to a listener only sees (and can load) the models assigned to that port; `/v1/models` is filtered per port.\n- **One shared GPU budget**: all listeners share ONE scheduler and VRAM accounting, so loading a model on one port can still evict a model loaded via another - there is a single GPU, not one per port. That shared accounting is the whole reason to use ports instead of running separate instances.\n- **Config**: the `listeners:` block maps a bind address to swap groups (groups are auto-derived from model-name globs at autogen time). Requires the group router.\n- **Access**: an API key can be scoped to specific models, which intersects with a port's catalog - see the api-keys article.\n\nChanging a listener's bind address needs a restart; its model scoping updates live on config save."
  },
  {
    "id": "playground-chat",
    "title": "Chat playground",
    "keywords": [
      "chat",
      "playground",
      "conversation",
      "vision",
      "image attach",
      "reasoning",
      "thinking",
      "rewrite",
      "history",
      "compaction"
    ],
    "body": "The Chat tab is a full chat client for your local models:\n\n- **Vision**: attach an image (paperclip) to a vision-capable model.\n- **Reasoning**: thinking models stream their reasoning into a collapsible box. A **Thinking Budget** (Settings) caps reasoning tokens so a model can't overthink forever.\n- **Web search**: optional - the model searches the web via SearXNG (see the web-search article).\n- **Wiki**: models can look up this help wiki to answer questions about Quartermaster.\n- **Rewrite mode**: send text with an instruction and get a side-by-side **word-level diff** (added / removed / changed) instead of a chat reply - good for editing prose or code in place.\n- **Runs server-side**: once you send, the answer is generated by the server, not your browser tab - so closing or refreshing the tab (or losing the connection) doesn't stop or lose it; reopen the chat and the in-progress answer is still streaming. Web search, wiki lookups, and tool approvals all continue server-side.\n- **History & auto-compaction**: chats are saved server-side per user. When a conversation reaches ~80% of the model's context window, the oldest turns (all but the most recent ~6 messages) are summarized by the model into a brief so the chat can continue past the raw context limit - the full transcript still shows in the UI, only what's *sent* to the model is compacted. A live bar shows how full the context is."
  },
  {
    "id": "playground-login",
    "title": "Playground accounts & login",
    "keywords": [
      "login",
      "log in",
      "sign in",
      "account",
      "username",
      "password",
      "register",
      "user",
      "auth",
      "playground port"
    ],
    "body": "The playground runs on its own port and is gated behind a per-user login (separate from the open dashboard).\n\n- **First login registers you**: type any username + password and sign in - an unknown username is created automatically. This keeps each person's chats, image/speech threads, and settings separate.\n- **Per-user storage**: your chat history and preferences are saved server-side under your account; nothing is shared between users.\n- **Not secure by design**: credentials are stored in plaintext for a local / trusted-network tool. Do NOT expose the playground port to the internet or reuse a real password - it's a convenience separator, not real authentication.\n- **Logout** is in the side rail, next to your username."
  },
  {
    "id": "web-search",
    "title": "Web search (SearXNG)",
    "keywords": [
      "search",
      "web",
      "searxng",
      "internet",
      "tool",
      "cors",
      "current",
      "news"
    ],
    "body": "Chat can let the model search the web for fresh or niche facts. It goes through a **SearXNG** instance you run.\n\nSetup (playground **Settings → Web Search**):\n1. Toggle Web Search on.\n2. Enter your SearXNG URL (e.g. `http://localhost:8888`) and hit **Test**.\n3. SearXNG must have **JSON format enabled** (`formats: [html, json]`).\n\n**Requirements** : the model must support **tool calling**.\nKnobs:\n- **Max/Turn** caps searches per message;\n- **Throttle ms** spaces requests so SearXNG's rate limiter doesn't trip;\n- **Dedupe** reuses the result for a repeated query.\n\nA bare \"Failed to fetch\" on Test is almost always CORS or a wrong host/port."
  },
  {
    "id": "youtube",
    "title": "YouTube transcripts",
    "keywords": [
      "youtube",
      "video",
      "transcript",
      "captions",
      "subtitles",
      "yt-dlp",
      "summarize"
    ],
    "body": "Paste a YouTube link in Chat and the model can read the video's captions and summarise, quote or fact-check it. The model calls a `youtube_transcript` tool; quartermaster fetches the captions server-side and hands them back as `[m:ss]` paragraphs, so the model can point at a moment (\"at 14:32 they claim ...\") and link it back with `&t=872s`.\n\n**Requirements**\n- The model must support **tool calling**.\n- **yt-dlp** must be present. The Windows installer offers it as an optional helper (tick *yt-dlp* on the tasks page) and drops it in `bin/yt-dlp`; otherwise install it yourself and leave it on `PATH`. Without it the tool answers with an install hint instead of a transcript.\n\n**Limits**\n- Only videos that actually have captions (manual or auto-generated). If captions are off, the tool says so rather than inventing content - there is no audio-transcription fallback yet.\n- Long videos are truncated to fit the context window; the result is marked `INCOMPLETE` and the model is told to say so.\n- 2 transcripts per message; each video is cached for 30 minutes.\n- Non-English captions: ask for them by language (e.g. \"get the German transcript\")."
  },
  {
    "id": "qm-tools",
    "title": "Quartermaster tools (assistant self-service)",
    "keywords": [
      "quartermaster tools",
      "qm tools",
      "inspect",
      "configure",
      "assistant",
      "introspection",
      "mcp",
      "self",
      "change settings",
      "approve",
      "approval",
      "diff",
      "agent config"
    ],
    "body": "**Quartermaster tools** let the chat assistant look at - and, with your approval, tune - the quartermaster instance it's running in. Toggle them per chat with the **QM Tools** switch in the chat menu (on by default, like Web Search). They only act on this running instance; they never load or unload models (that would evict the very model you're talking to).\n\n**Inspect (read-only).** The assistant can pull a compact, formatted view of your setup: what models are installed and their capabilities, what's loaded right now, live GPU/VRAM and RAM, the global memory settings, or one model's full config. Ask things like \"what models do I have?\", \"how much VRAM is free?\", or \"show my Qwen model's context and offload\".\n\n**Configure (approval-gated).** The assistant can change three kinds of settings:\n- **Global memory settings** - VRAM target, headroom, max RAM, idle-unload TTL (the dashboard Settings).\n- **Your playground preferences** - temperature, max tokens, thinking budget, web search on/off, and the search knobs. These are your own per-user prefs and take effect on your next page reload.\n- **A model's config** - context, KV quant, CPU offload, and the like.\n\n**You approve every change.** When the assistant wants to configure something, the chat pauses and shows a **before → after diff card** with **Accept** and **Deny**. Nothing is applied until you accept; deny (or ignore it - it times out after 5 minutes) and nothing happens. Accepting a global or model change hot-reloads the config in place - running models are **not** evicted, and a changed model's new launch args apply the next time it loads.\n\nQuartermaster tools need the server running with config editing enabled (`-generate`)."
  },
  {
    "id": "images",
    "title": "Image generation",
    "keywords": [
      "image",
      "images",
      "sd",
      "stable diffusion",
      "txt2img",
      "img2img",
      "inpaint",
      "hires",
      "kontext",
      "flux",
      "cfg",
      "blurry"
    ],
    "body": "The Images tab drives stable-diffusion.cpp models:\n\n- **txt2img** and **img2img** (needs a source image + a denoise strength).\n- **Hires fix**: a second upscaled pass for sharper detail (txt2img only).\n- **Reference images** (Kontext-class models): condition on a subject/style image while the prompt drives the edit.\n- **Inpainting**: open the mask editor to brush over the area to change (adjustable brush; white = regenerate, black = keep), then generate - only the painted region is redrawn.\n- **Style presets** keep a batch visually consistent.\n- **Upscale**: the ⤢ button on any generated image (or on an attached/uploaded image in the composer) runs a standalone ESRGAN upscaler - see the Upscaling article.\n\nTips: **turbo/lightning** checkpoints usually need **CFG = 1.0** - a higher CFG makes them blurry. SDXL/SD models need a **full checkpoint** (they can't be split-loaded from separate UNet + text-encoder files)."
  },
  {
    "id": "upscale",
    "title": "Upscaling images",
    "keywords": [
      "upscale",
      "upscaling",
      "esrgan",
      "realesrgan",
      "ncnn",
      "ultrasharp",
      "4x",
      "enlarge",
      "super resolution",
      "sharpen",
      "enhance"
    ],
    "body": "Quartermaster can upscale any image with an ESRGAN model (e.g. 4x-UltraSharp) via the **realesrgan-ncnn-vulkan** runner.\n\n**In the Images tab:** click the ⤢ (Maximize) button on any generated image's action row, or hover an attached/uploaded image in the composer and click its ⤢ button. The upscaled result (4×) posts as a new turn below. One upscale runs at a time.\n\n**API:** `POST /v1/images/upscale` with JSON `{ \"image\": \"<data-url or base64>\", \"scale\": 4, \"model\": \"<optional model name>\" }` → `{ \"image\": \"data:image/png;base64,…\", \"model\", \"scale\" }`. Auth-gated like the inference API. Scale 2–4 (4x-UltraSharp is 4×-only). When `model` is omitted the server picks the first discovered upscaler.\n\n**How it works:** the upscaler is exec-per-request - not a loaded/swapped model, so it holds no persistent VRAM and doesn't evict anything. It's configured as a backend of **kind `upscale`** (its exe path); ncnn model files (`<name>.param` + `<name>.bin`) live in a `models/` folder beside the exe. A tile cap keeps peak VRAM low so an upscale coexists with a loaded generation model."
  },
  {
    "id": "segmentation",
    "title": "Image segmentation (SAM)",
    "keywords": [
      "segment",
      "segmentation",
      "sam",
      "sam3",
      "mask",
      "cutout",
      "object",
      "detect",
      "select object"
    ],
    "body": "Quartermaster serves **SAM (Segment Anything)** models through the `sam3_server` backend for object segmentation - producing masks/cutouts of objects in an image.\n\n**API:** `POST /v1/segment` with the model id in the JSON body plus the input image and prompt (point/box/text depending on the model). The model is discovered from a `.ggml` file and served like any other backend - the first request loads it. SAM runs as a **coexisting persistent group** (it isn't swapped out by text/image models sharing the GPU).\n\n**Backend:** configured as a backend of **kind `sam`** (the `sam3_server` exe path) in the backend registry. See the Backends article for adding backend executables."
  },
  {
    "id": "speech-audio",
    "title": "Speech and transcription",
    "keywords": [
      "speech",
      "tts",
      "voice",
      "audio",
      "transcribe",
      "whisper",
      "stt",
      "text to speech",
      "voice cloning",
      "clone voice",
      "voice design",
      "speaker",
      "preset",
      "studio"
    ],
    "body": "Two playground tabs cover audio.\n\n**Speech (TTS)** is a small studio: type text on the left, pick a voice, and each generation becomes a waveform card in the clip library on the right. Cards can be played, edited (which regenerates), regenerated, downloaded (WAV), or deleted; a volume slider and Auto-play toggle sit pinned at the bottom, and clips are saved per user as threads.\n\nHow voices work depends on the model kind (auto-detected):\n- **Base model** - one default voice, plus **voice cloning**: click *Clone a voice* and either read the on-screen passage aloud (live mic) or upload a WAV/MP3 sample; an optional reference transcript improves the clone. Cloned voices are saved and reusable, and can be deleted.\n- **Custom-voice model** - ships its own set of named speakers; pick one from the list (no default).\n- **Voice-design model** - no fixed speakers. Instead you pick a **style preset** (a written description like \"a calm elderly man with a slight rasp\"). Several presets ship built-in, and you can *Design a voice* to save your own.\n\nUse the refresh button by the voice list to reload the voices a model actually offers.\n\n**Transcription (STT)** - upload an audio file and get the transcript from a speech-recognition model.\n\nBoth use the same on-demand loading as every other model - the first request loads the model."
  },
  {
    "id": "rerank-embed",
    "title": "Rerank and embeddings",
    "keywords": [
      "rerank",
      "reranker",
      "embedding",
      "embed",
      "search",
      "relevance"
    ],
    "body": "Rerankers and embedding models have no playground UI - they are API-only.\n\n**Rerank**: a reranker model is served on `/v1/rerank`; give it a query and a list of documents and it scores each document's relevance, so an external app (a RAG pipeline) can order results.\n\n**Embeddings**: embedding models are served on the OpenAI-compatible `/v1/embeddings` endpoint for use by external apps (vector search, semantic similarity)."
  },
  {
    "id": "api-keys",
    "title": "API keys and access",
    "keywords": [
      "api",
      "key",
      "auth",
      "token",
      "openai",
      "access",
      "scope",
      "endpoint"
    ],
    "body": "Quartermaster serves an **OpenAI-compatible API** (`/v1/chat/completions`, `/v1/embeddings`, image and audio routes), so existing OpenAI clients work by pointing at your server's URL.\n\nThe **API Keys** page (dashboard) creates keys and optionally **scopes** each key to a subset of models. Keys gate inference and model discovery; the dashboard itself stays open. Point any OpenAI SDK at the server base URL and use your key as the API key."
  },
  {
    "id": "tools-api",
    "title": "Tools API (search & YouTube for your own apps)",
    "keywords": [
      "tools api",
      "v1/tools",
      "http",
      "endpoint",
      "external",
      "app",
      "integration",
      "custom",
      "execute",
      "search",
      "youtube",
      "transcript",
      "comments",
      "providers",
      "duckduckgo",
      "searxng",
      "brave",
      "yt-dlp"
    ],
    "body": "Besides chat, quartermaster exposes the same tool **executors** the playground uses - web search and YouTube - as plain HTTP endpoints, so your own scripts, agents, and apps can call them instead of wiring up SearXNG, DuckDuckGo, or yt-dlp themselves.\n\nThey live on the `/v1` surface, so they are gated by the **same API keys** as inference (see *API keys and access*). Any valid key works; model scopes do not apply.\n\n**Endpoints** (all `POST`, JSON in and out):\n\n- `POST /v1/tools/search` - `{\"query\": \"...\", \"limit\": 5, \"providers\": [...]}` → `{\"provider\": \"...\", \"results\": [{title, url, content}]}`. **Providers are passed per call** - no config needed: `{\"id\":\"duckduckgo\"}` works keyless; also `{\"id\":\"searxng\",\"baseUrl\":\"http://localhost:8080\"}`, `{\"id\":\"brave\",\"key\":\"…\"}`, `{\"id\":\"tavily\",\"key\":\"…\"}`, `{\"id\":\"google\",\"key\":\"…\",\"cx\":\"…\"}`. The chain fails over until one answers. `limit` is 1–10.\n- `POST /v1/tools/youtube/transcript` - `{\"url\": \"https://youtu.be/…\"}` (or a bare 11-character video id), optional `lang` → `{id, title, uploader, duration, text}` with `[m:ss]` timestamps. Returns the **full** transcript - long videos come back long.\n- `POST /v1/tools/youtube/search` - `{\"query\": \"…\"}` or `{\"channel\": \"@handle\"}` (optional `tab`, `limit`) → `{\"videos\": [{id, title, channel, duration, views, …}]}`.\n- `POST /v1/tools/youtube/comments` - `{\"url\": \"…\"}` + `limit` → `{\"video\": {…}, \"comments\": [{author, text, likes, pinned, by_owner}]}`.\n\n**Requirements** - the same as chat: the YouTube endpoints need **yt-dlp** (they answer `503` with an install hint when it is missing); transcripts need a video that actually has captions.\n\n**Errors** are OpenAI-shaped `{\"error\": {\"message\": \"…\"}}`: `400` bad arguments, `502` upstream failure, `503` yt-dlp missing.\n\nTypical use: your app keeps its own tool schemas for the model and executes each tool call here. Example:\n\n```\ncurl -H \"Authorization: Bearer qm-…\" -H \"Content-Type: application/json\" \\n     -d '{\"query\":\"llama.cpp release\",\"providers\":[{\"id\":\"duckduckgo\"}]}' \\n     http://127.0.0.1:1250/v1/tools/search\n```"
  },
  {
    "id": "observe",
    "title": "Observe: activity, logs, performance, KV cache",
    "keywords": [
      "observe",
      "logs",
      "activity",
      "metrics",
      "performance",
      "kv cache",
      "monitor",
      "debug",
      "tokens per second"
    ],
    "body": "The **Observe** page has tabbed monitoring:\n\n- **Activity**: per-request history - path, status, token counts, throughput, duration; drill into a captured request/response.\n- **Logs**: live proxy + upstream (llama-server) logs.\n- **Performance**: CPU, system RAM, and GPU charts over time.\n- **Context → KV Cache**: how much of each model's context window is filled, plus KV-cache disk-save hit/miss stats and prompt-canonicalization stats.\n\nLive metrics stream over SSE, so the page updates in real time while a model generates."
  },
  {
    "id": "gpu-memory",
    "title": "GPU memory / VRAM",
    "keywords": [
      "vram",
      "gpu",
      "memory",
      "oom",
      "out of memory",
      "offload",
      "budget",
      "foreign"
    ],
    "body": "Quartermaster tracks GPU memory live and breaks it down (System / Weights / Draft / KV / Checkpoints / CUDA / Foreign). The VRAM budget already accounts for memory the system and other apps hold, so its estimates target *free* VRAM.\n\n- **Foreign**: VRAM held by llama-server/sd-server processes Quartermaster didn't spawn (a stray llama.cpp) - it counts these so it won't overcommit.\n- If a model won't load or crashes on load, the usual cause is **not enough free VRAM**. Lower that model's **Target VRAM** (more layers to CPU) or its **context size** in the cogwheel editor, or unload other models."
  },
  {
    "id": "slot-kv-cache",
    "title": "KV-cache disk save (survive eviction)",
    "keywords": [
      "kv cache",
      "kv-cache",
      "slot",
      "disk",
      "save",
      "restore",
      "prefill",
      "reprefill",
      "reprocess",
      "slot-save-path",
      "persist",
      "checkpoint",
      "hybrid",
      "recurrent",
      "conversation id",
      "eviction"
    ],
    "body": "**KV-cache disk save** (dashboard Settings → KV Cache) persists a conversation's KV cache to disk so a long prompt isn't re-processed from scratch after the model is evicted.\n\nWhy: llama-server has a single slot - any new request can push out the resident conversation. Normally returning to that chat means re-prefilling the whole prompt, which for a 30k+ context can take minutes. With this on, Quartermaster snapshots the KV before eviction and restores it instead. Measured on a 27B model at 32k context: prefill dropped from 60.6s to 0.35s, with 32,032 of 32,057 tokens reused across a full process restart.\n\n- **Turn it on**: two gates - the global **Enable** switch (plus Directory, min-tokens-to-save, max disk GB, max sessions) in Settings → KV Cache, AND a per-model **Save KV cache to disk** checkbox in the cogwheel editor. A model without the checkbox is left alone.\n- **What counts as one conversation**: each conversation gets its own snapshot file. The playground sends a stable per-chat id, so a new chat is always a new file. Other clients (pi, Aider, anything using a plain OpenAI SDK) send no id, so the conversation is identified by a hash of its **first system + first user message**. Two sessions that open with the exact same text therefore share one file. That is safe - the restored prefix is validated, so the worst case is a slower request, never a wrong answer, and the shared opening is usually reused for free. The only real cost is that the two sessions overwrite each other's snapshot, so only one of them gets a full restore later.\n- **Check it's working**: Observe → Context → KV Cache lists saved files and confirm-hit / confirm-miss counts - a *confirm* means the restored KV was actually reused on the next request, not just loaded.\n- **Disk cost**: roughly 40 KB per token, so a 60k-token session is around 2.4 GB. The max-disk-GB and max-sessions caps evict the least recently used snapshots.\n\n**Hybrid / recurrent models (Qwen3.5 / 3.6 / 3.8) work, with one limit.** These architectures keep a rolling state rather than a per-token history, so a saved state can be continued *forward* from exactly where it was saved, but never rewound to an earlier point. In practice that is fine - chats only ever grow - so save/restore gives the same speedup as on standard models. Two narrower paths are skipped automatically for them:\n\n- **Preamble seeding** (pre-warming a brand-new chat with a shared system+tools prefix) is off, because that shortcut requires rewinding.\n- **Restoring a snapshot larger than the incoming prompt** is skipped, since that also needs a rewind and would reuse nothing. This only comes up if a client edits or truncates its own history.\n\nStandard transformer models get every path, including preamble seeding."
  },
  {
    "id": "settings",
    "title": "Playground settings (tokens, thinking)",
    "keywords": [
      "settings",
      "max tokens",
      "thinking budget",
      "reasoning",
      "temperature",
      "length",
      "dashboard",
      "memory",
      "vram",
      "backends",
      "idle unload"
    ],
    "body": "Playground **Settings** (gear in the side rail):\n\n- **Max Tokens**: cap on a single response's length.\n- **Thinking Budget**: max reasoning tokens before a thinking model is forced to answer - stops it overthinking. 0 = unlimited. It's enforced as a clean server-side stop so the model's KV cache stays warm.\n- **Web Search**: see the web-search article.\n- **Temperature** (per-chat): higher = more varied/creative, lower = more focused.\n\nThese are the **playground** per-user settings (saved to your account; the assistant's Quartermaster tools can adjust them for you). The **dashboard** has a separate Settings modal for server-wide config - memory budget (Target VRAM / Headroom / Max RAM), idle unload, KV-cache disk save, and Backends - each covered in its own article."
  },
  {
    "id": "known-issues",
    "title": "Known issues & hardware limits",
    "keywords": [
      "known issue",
      "amd",
      "radeon",
      "vulkan",
      "rocm",
      "hip",
      "flux",
      "sdxl",
      "out of device memory",
      "compute buffer",
      "cuda null",
      "ggml_cuda_init",
      "500",
      "generate_image returned no results",
      "buffer size limit",
      "nvidia",
      "gpu limit"
    ],
    "body": "Known limitations and their workarounds, mostly GPU/backend specific.\n\n**AMD + Vulkan: large image models (Flux, SDXL hi-res) fail to allocate.**\nSymptom: image gen returns `500 - generate_image returned no results`; sd-server log shows `ggml_vulkan: Requested buffer size exceeds device buffer size limit: ErrorOutOfDeviceMemory` and `flux: failed to allocate the compute buffer`. Cause: AMD's Windows Vulkan driver caps a *single* memory allocation at **2 GiB** (`maxMemoryAllocationSize`/`maxBufferSize`), regardless of how much VRAM is free. Flux/SDXL need one contiguous compute buffer larger than that at higher resolutions, so it is rejected even with 20+ GB free. `--diffusion-fa` (flash attention) is already on and cannot bring it under 2 GiB at high res.\n- Workaround: lower the generation resolution (768x768 usually works, 512 is safe). Smaller/distilled diffusion models stay under the cap - e.g. Z-Image-Turbo works fine on Vulkan.\n- Real fix for hi-res Flux/SDXL on AMD: use a ROCm/HIP sd-server build (HIP has no 2 GiB single-allocation cap). Set it in Settings -> Backends. Text (llama-server) is unaffected because model weights are split across many sub-2 GiB buffers; only large single diffusion tensors hit the wall.\n\n**`ggml_cuda_init: failed to initialize CUDA: (null)` on every model load.**\nThis means the backend binary you are running is a **CUDA-compiled** build, but no NVIDIA GPU is present (e.g. on an AMD box). At startup llama.cpp/sd.cpp probe for a CUDA device, fail, log this line, then fall back to CPU (slow). It is harmless as a message but means that process is NOT on the GPU. Fix: point Settings -> Backends at a Vulkan (or ROCm) build. A binary compiled without CUDA never prints this line.\n\n**AMD GPUs report VRAM only - no temperature / fan / power.**\nOn non-NVIDIA Windows GPUs, quartermaster reads VRAM (total/used) and utilization via DXGI + PDH, but the driver does not expose temp/fan/power the way nvidia-smi does, so those gauges stay blank. Expected, not a bug.\n\n**Choosing Vulkan vs ROCm/HIP on AMD.** For text (llama-server), Vulkan is the easy, working default. For image generation, prefer ROCm/HIP where you need higher resolution, because of the Vulkan 2 GiB single-allocation cap above."
  },
  {
    "id": "troubleshooting",
    "title": "Troubleshooting common issues",
    "keywords": [
      "problem",
      "error",
      "help",
      "not working",
      "slow",
      "crash",
      "wont load",
      "fails",
      "broken",
      "fix"
    ],
    "body": "Common issues and fixes:\n\n- **Model won't load / crashes on load** → not enough free VRAM. Lower its Target VRAM or context in the cogwheel editor, or unload other models.\n- **Generation is slow** → too many layers on CPU (low VRAM target), or context is very large. Raise Target VRAM if you have headroom.\n- **Web search fails (\"Failed to fetch\")** → SearXNG unreachable, CORS-blocked, or JSON format not enabled. Re-check the URL with Test.\n- **Blurry images** → turbo/lightning checkpoints need CFG = 1.0. SDXL needs a full checkpoint file.\n- **Model swaps unexpectedly** → another request pulled in a group-mate and evicted yours; give heavy models their own swap group or enough VRAM to coexist.\n- **Config change didn't take effect** → running models keep old settings until they reload; the staging card shows what's actually loaded."
  }
]
