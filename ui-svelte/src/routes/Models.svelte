<script lang="ts">
  import ModelsPanel from "../components/ModelsPanel.svelte";
</script>

<!-- The Models page now owns its own two-section layout (active panel + card
     grid); logs live inside the active panel, so no outer split here. -->
<ModelsPanel />
